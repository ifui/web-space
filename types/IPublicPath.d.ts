/**
 * 资源线上路径配置
 * @param output 输出路径
 * @param js JS线上路径
 * @param css CSS线上路径
 * @param iconfont Iconfont线上路径
 * @param image Image线上路径
 */
export interface IPublicPath {
  output?: string
  js?: string
  css?: string
  iconfont?: string
  image?: string
}
